import React from 'react';
import PropTypes from 'prop-types';

const Order = () => (

  <div>
    <h2>New Order</h2>
  </div>
);

Order.propTypes = {
  id: PropTypes.string,
};

export default Order;
